import React, { useState } from "react";
import api from "../api";

export default function GoalList() {
    const listElements = <li>{1}</li>;
    return (
        <ul>
            {listElements}
        </ul>
    )
}